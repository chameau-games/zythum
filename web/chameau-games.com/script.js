
/* PROJECTS */
/*
var elementsG = document.getElementsByClassName('card-game');

function ongame(game_nb) {
    var gameCard = elementsG[game_nb];
    gameCard.classList.add("selected")
}

function outgame(game_nb) {
    var gameCard = elementsG[game_nb];
    gameCard.classList.remove("selected")
}
*/

/* TEAM */

var elementsM = document.getElementsByClassName('card-member');

function onmember(member_nb) {
    var memberCard = elementsM[member_nb];
    memberCard.classList.add("selected")
}

function outmember(member_nb) {
    var memberCard = elementsM[member_nb];
    memberCard.classList.remove("selected")
}
